import { createTheme } from '@mui/material/styles'
const theme = createTheme({
    typography: {
        fontFamily: 'Poppins'
    },
    palette: {
        primary: {
            light: '#FFFFFF',
            main: '#34C94B',
            dark: '#121212',
        }
    }
})

export default theme;